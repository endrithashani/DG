1.Add a loader
2. Change the header
3. Change the slideshow
4. Add images to spans
5. Add buttons to services container and honver for both the button and the container
6. Add borders to the containers under benifits
7. Change the contact form and validate using JavaScript not the validation built in on HTML also shows the errors
8 . Add a subscribe to the newletter to replace the section above the footer
9. Added a new scroll to top with progress bar 